{
  "name": "Run DescluB Prototype",
  "description": "Install dependencies and start dev server",
  "steps": [
    {
      "type": "shell",
      "command": "npm install && npm run dev -- --host 0.0.0.0 --port 3000"
    }
  ]
}
